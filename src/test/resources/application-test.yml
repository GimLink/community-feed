spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./community_feed
    username: sa
    password: 1234

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
